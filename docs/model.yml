# Data model

Topic:
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  name: String!

Question:
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  position: Number!
  body: String!
  help: String

Resource:
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  label: String!
  url: String!

Response:
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  question:
