import React from 'react'
import StyledCurrentTopic from '../styled/CurrentTopic'

export default function CurrentTopic ({ currentTopic }) {
  return <StyledCurrentTopic>{currentTopic}</StyledCurrentTopic>
}
