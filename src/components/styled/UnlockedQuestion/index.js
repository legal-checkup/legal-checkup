// import styled from 'styled-components'

// const UnlockedQuestion = styled.span`
//   border: none;
//   color: ${({ onClick }) => (onClick ? 'black' : 'yellow')};
//  `

// UnlockedQuestion.displayName = 'StyledUnlockedQuestion'

// export default UnlockedQuestion
