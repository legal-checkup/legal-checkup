// import styled from 'styled-components'

// const PreviousQuestion = styled.span`
//   border: none;
//   color: ${({ onClick }) => (onClick ? 'yellow' : 'red')};
//  `

// PreviousQuestion.displayName = 'StyledPreviousQuestion'

// export default PreviousQuestion
