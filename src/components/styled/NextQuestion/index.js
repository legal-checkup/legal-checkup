// import styled from 'styled-components'

// const NextQuestion = styled.span`
//   border: none;
//   color: ${({ onClick }) => (onClick ? 'yellow' : 'black')};
//  `

// NextQuestion.displayName = 'StyledNextQuestion'

// export default NextQuestion
