// import styled from 'styled-components'

// const ActiveQuestion = styled.span`
//   border: box;
//   color: ${({ onClick }) => (onClick ? 'red' : 'black')};
//  `

// ActiveQuestion.displayName = 'StyledActiveQuestion'

// export default ActiveQuestion
