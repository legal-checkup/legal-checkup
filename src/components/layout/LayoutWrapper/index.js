import styled from 'styled-components'

const LayoutWrapper = styled.div`
  position: relative;
  min-height: 100vh;
  background-color: #eeedea;
`

LayoutWrapper.displayName = 'StyledLayoutWrapper'

export default LayoutWrapper
